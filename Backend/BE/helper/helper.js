const messageHelper = (result, status, message) => {
    return {
        message: message,
        status: status,
        result: result,
    };
};

// export default {
// messageHelper,
// }
export default {messageHelper}