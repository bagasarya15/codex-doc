//Penggunaan tupleType jika nilai arraynnya fixs
let skill : [string, string]
skill = ['programing', 'js']

skill.push('DB')
console.log(skill);

let color: [number, number, number] = [255, 0, 0];
console.log(color);

let bgColor: [number, number, number, number?];
bgColor = [0, 255, 255, 0.5];
bgColor = [0, 255, 255];
console.log(bgColor);